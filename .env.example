# Port the dev server listens on (defaults to 5173)
PORT=5173

# URL of the backend API server (used to proxy /api/* requests in dev)
# Set this to your deployed API URL for production contact form submissions
VITE_API_URL=http://localhost:3001
